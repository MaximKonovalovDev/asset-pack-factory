# BOARD — one board, zero copies.

Live board: http://127.0.0.1:20128/dashboard (Cook Jobs card inside).

Why there and not here: the board is served BY the gateway program.
Code that runs stays where it runs (forge). Everything you touch
(packs, briefs, cook, skills, books) lives HERE. One board, no mirrors,
mirrors rot. This file is a pointer, not a copy.
