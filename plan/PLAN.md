# PLAN — clean repo, clean flow

Root has 3 files only: AGENTS.md, README.md, opencode.jsonc.
All guides live in docs/. All work has owner + gate.

```
apf/ (pipe) exports/ (engines) ship/ (sites)
packs/ (briefs) work/ (raw) previews/ (shots+mp4)
sfx/ vfx/ research/ records/ state/ docs/ plan/
```

Flow: brief -> gen -> fix -> pbr -> lod -> verify -> export -> preview -> ship.
Gate red = stop. No skip.
